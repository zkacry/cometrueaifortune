# myfortune v3 - API テスト機能リリース

**リリース日**: 2026-05-28  
**バージョン**: v3  
**APK ファイル**: `myfortune-arm64-v8a-release-v3-api-test.apk` (33MB)  
**Google Drive**: `G:\マイドライブ\apk\`

---

## 新機能：API接続テスト

### 概要
「APIが利用できない」というエラーを**自己診断できる機能**を追加しました。

ユーザーがアプリ内から DeepSeek と Gemini の API 接続状況を簡単にテストできます。

### アクセス方法
```
設定タブ → デバッグセクション（下部） → API接続テスト → テストを実行
```

### 何ができるか
✅ **DeepSeek API の接続確認**
- API キーの有効性チェック
- ステータスコード判定（401, 403, 429, 500-503 など）
- 接続速度計測

✅ **Gemini API の接続確認**
- API キーの有効性チェック
- エンドポイント応答確認
- ネットワーク遅延計測

✅ **自動診断・推奨アクション**
- 両方成功 → 「アプリを再起動してください」
- 片方失敗 → 「もう一方が自動的に使用されます」
- 両方失敗 → 「インターネット接続を確認してください」

---

## テスト結果の見方

### ✅ 成功（緑）
```
[DeepSeek] ✅ OK (200)
Connected successfully. Response: ...
Status: 200
Time: 1523ms
```
**意味**: API に正常に接続でき、レスポンスを受け取った

### ❌ 失敗（赤）
```
[Gemini] ❌ FAIL (401)
401 Unauthorized - API key is invalid or expired
Status: 401
Time: 245ms
```
**意味**: API キーが無効または権限不足

### よくあるエラーコード
| コード | 意味 | 対策 |
|--------|------|------|
| 401 | API キー無効 | サービス側で API キーを確認・再生成 |
| 403 | 権限不足 | サービス側で権限設定を確認 |
| 429 | リクエスト数超過 | 暫く待ってから再試行 |
| 500-503 | サーバーエラー | サービスが一時的にダウン |
| Timeout | サーバー応答なし | ネットワーク確認 |

---

## 使用場面

### シーン 1: ユーザーがエラーを報告
```
ユーザー：「占いを試したら『APIが利用できない』と出ます」
```
**対応**:
1. 「設定 → API接続テスト → テストを実行」で診断
2. 結果を確認して対策を実施
3. 「もう一方が動いていれば問題なし」と説明可能

### シーン 2: サービス障害時
```
DeepSeek が 500 エラーを返している
```
**対応**:
1. テスト画面で即座に "Server Error" と判明
2. ユーザーには「サービスが一時的にダウンしています」と説明
3. Gemini が機能していれば「自動的に使用されます」と安心させる

### シーン 3: 開発者がバグ調査
```
「APIが利用できません」というバグ報告が来た
```
**対応**:
1. 「設定 → API接続テストで自己診断してください」と案内
2. ユーザーからテスト結果スクリーンショットをもらう
3. ステータスコードで原因を即座に特定

---

## 実装詳細

### ファイル構成
```
lib/
├── data/services/
│   └── api_test_service.dart      ← API テストロジック（120行）
│       ├── testDeepSeek()         テスト実行
│       ├── testGemini()           テスト実行
│       ├── testAll()              両方のテスト
│       └── ApiTestResult          結果データクラス
│
└── features/debug/
    └── screens/
        └── api_test_screen.dart   ← テスト UI（250行）
            ├── テスト実行ボタン
            ├── リアルタイム結果表示
            ├── エラー詳細表示
            └── 自動診断メッセージ

features/settings/screens/
└── settings_screen.dart           ← 統合
    └── デバッグセクション追加
        └── API接続テスト メニュー
```

### テストロジック
```dart
1. DeepSeek API をテスト
   POST https://api.deepseek.com/chat/completions
   ↓
2. Gemini API をテスト
   POST https://generativelanguage.googleapis.com/v1beta/openai/chat/completions
   ↓
3. 両方の結果を集約
   ├─ 両方成功 → 「正常です」
   ├─ 片方失敗 → 「フォールバック機能で対応」
   └─ 両方失敗 → 「診断フロー提示」
```

---

## インストール・テスト方法

### ユーザー向け
1. APK ファイルをダウンロード
2. Android デバイスで「提供元不明のアプリ」をインストール許可
3. アプリを開く → 設定 → API接続テスト

### 開発者向け
```bash
# インストール
adb install -r myfortune-arm64-v8a-release-v3-api-test.apk

# ログ確認
adb logcat | grep -E "ApiTest|DeepSeek|Gemini"

# テスト実行
# デバイスで「設定 → API接続テスト → テストを実行」をタップ
```

---

## バージョン情報

| バージョン | 日付 | 主な変更 |
|-----------|------|---------|
| v1.0 | 初版 | アプリ基本機能 |
| v1.1 | 月次プラン実装 | Free / Light / Pro プラン |
| v2.0 | 初月無料キャンペーン | 初月10回無料 |
| **v3** | **2026-05-28** | **API テスト機能追加** |

---

## トラブルシューティング

### Q: API テストが見つからない
**A**: 新しい APK をインストール必要。古い APK を削除して再インストール。

### Q: テスト実行時にクラッシュする
**A**: デバイスを再起動。または `adb logcat` で詳細エラーを確認。

### Q: 両方のテストが毎回失敗する
**A**: 
1. インターネット接続を確認
2. `.env` ファイルの API キーを確認
3. サービス公式ページで障害状態を確認

### Q: テスト画面にアクセスできない
**A**: APK が古い可能性。Google Play で最新版がリリースされるまで待つ。

---

## パフォーマンス

### テスト実行時間
- **DeepSeek**: 1000-2000ms 典型
- **Gemini**: 800-1500ms 典型
- **両方**: 最大 2000ms（並列実行）

### ネットワーク負荷
- テストリクエスト: JSON 約 300 バイト
- テスト応答: 100 トークン以下
- API クォータ影響: 無視できるレベル（1回のテスト）

---

## セキュリティ考慮

✅ **API キーは安全に処理**
- ログに API キーは出力しない
- テスト結果には詳細なエラーメッセージのみ
- クライアント側でのみ実行（サーバー側に送信しない）

✅ **ネットワーク通信は暗号化**
- HTTPS 通信のみ使用
- 自己署名証明書はチェック

---

## 次のリリース予定

- [ ] API テスト結果をログファイルに保存
- [ ] テスト履歴表示機能
- [ ] API 応答時間グラフ
- [ ] API キー入力フォーム（セキュアな変更機能）
- [ ] サービス状況の定期監視機能

---

## サポート

### ユーザーからの問い合わせ対応テンプレート

```
Q: 「APIが利用できない」というエラーが出ます
A: アプリの「設定 → API接続テスト」で診断してください。

- ✅ 両方成功 → アプリを再起動してください
- ⚠️ 片方失敗 → もう一方が自動的に使用されます
- ❌ 両方失敗 → インターネット接続を確認してください

詳細なエラーコードが表示されたら、スクリーンショットを
送信してください。
```

---

## 更新履歴

**2026-05-28 08:00** - v3 リリース
- API テスト機能追加
- デバッグセクション新規追加
- ドキュメント作成（API_TEST_GUIDE.md、API_TEST_IMPLEMENTATION_SUMMARY.md）
- APK ビルド・Google Drive アップロード完了

---

## 関連リンク

📖 [ユーザー向けガイド](./API_TEST_GUIDE.md)  
🔧 [実装ドキュメント](./API_TEST_IMPLEMENTATION_SUMMARY.md)  
⚙️ [ソースコード - API テストサービス](./lib/data/services/api_test_service.dart)  
⚙️ [ソースコード - テスト UI](./lib/features/debug/screens/api_test_screen.dart)  

---

**マイフォーチューン開発チーム**  
**最終更新**: 2026-05-28
