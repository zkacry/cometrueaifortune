name: myfortune
description: "AI記録占い - あなたの記憶を読んで占うAIアプリ"
publish_to: 'none'

version: 1.0.3+2104

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 状態管理
  flutter_riverpod: ^2.6.1

  # Firebase
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.2
  cloud_firestore: ^5.6.5

  # ローカルDB
  sqflite: ^2.4.2
  path_provider: ^2.1.5
  path: ^1.9.1

  # HTTP�E�Elaude API�E�E  dio: ^5.8.0+1

  # 環墁E��数
  flutter_dotenv: ^5.2.1

  # 日付�E数値
  intl: ^0.20.2

  # ローカル保孁E  shared_preferences: ^2.5.3

  # グラチE  fl_chart: ^0.70.2

  # プッシュ通知
  flutter_local_notifications: ^17.1.3
  permission_handler: ^12.0.3

  # アプリ冁E��釁E  in_app_purchase: ^3.2.0

  # AdMob庁E��
  google_mobile_ads: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icons/icon_app.jpg"
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/icons/
